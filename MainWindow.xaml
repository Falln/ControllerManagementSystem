<Window x:Class="ControllerManagementSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ControllerManagementSystem"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Icon="/CMSFavicon.ico"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        FontSize="14"
        mc:Ignorable="d"
        MouseDown="FocusGrid"
        Title="ControllerManagementSystem" Height="450" Width="800" ResizeMode="CanResize">
    <Window.Resources>
        <!-- Default Colors -->
        <Color x:Key="BaseGray">#FF2A2A2A</Color>
        <Brush x:Key="BaseBackground">#FF2A2A2A</Brush>

        <Style TargetType="{x:Type Button}" x:Key="WindowButtons">
            <Setter Property="Background" Value="{StaticResource BaseBackground}"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF525252"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>
    <Border BorderThickness="0">
        <Grid x:Name="Grid" Focusable="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Window interaction buttons -->
            <!--
            <Button x:Name="minimizeBtn" Style="{StaticResource WindowButtons}" Grid.Column="5" Grid.Row="0" Click="minimizeBtn_Click">
                <Image Source="/Images/subtract.png" />
            </Button>
            <Button x:Name="makeResizeBtn" Style="{StaticResource WindowButtons}" Grid.Column="6" Grid.Row="0" Click="makeResizeBtn_Click">
                <Image Source="/Images/restore-down.png" />
            </Button>
            <Button x:Name="closeBtn" Style="{StaticResource WindowButtons}" Grid.Column="7" Grid.Row="0" Click="closeBtn_Click">
                <Image Source="/Images/close.png" />
            </Button>
            -->

            <TextBox x:Name="UsernameCheckoutBox" 
                    materialDesign:HintAssist.Hint="Name/Tag" 
                    materialDesign:HintAssist.IsFloating="True"
                    ToolTip="Person checking the controller out" 
                    TextWrapping="Wrap" 
                    Grid.Column="0" Grid.Row="1" Margin="20,0,0,0" VerticalAlignment="Top"
                    Width="120"/>
            <!-- <Label Content="Type" Grid.Column="1" Grid.Row="0" Margin="5,0,0,3" VerticalAlignment="Bottom"/> -->
            <ComboBox x:Name="ControllerTypeBox"
                      SelectionChanged="ControllerTypeBox_SelectionChanged"
                      DropDownClosed="FocusGrid"
                      materialDesign:HintAssist.Hint="Type" 
                      materialDesign:HintAssist.IsFloating="True"
                      HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"
                      Grid.Column="1" Grid.Row="1" Margin="10,0,0,0" VerticalAlignment="Top"
                      Width="70"/>
            <!-- <Label Content="Controller Name" Grid.Column="2" Grid.Row="0" Margin="5,0,0,3" VerticalAlignment="Bottom"/> -->
            <ComboBox x:Name="ControllerNumberBox" 
                      materialDesign:HintAssist.Hint="Name" 
                      materialDesign:HintAssist.IsFloating="True"
                      SelectionChanged="ControllerNumberBox_SelectionChanged"
                      DropDownClosed="FocusGrid"
                      HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom"
                      Grid.Column="2" Grid.Row="1" Margin="10,0,0,0" VerticalAlignment="Top"
                      Width="140"/>
            <TextBox x:Name="InitialsBox" 
                     materialDesign:HintAssist.Hint="Initials" 
                     materialDesign:HintAssist.IsFloating="True"
                     ToolTip="Employee Initials" 
                     Grid.Column="3" Grid.Row="1" Margin="10,0,0,0" VerticalAlignment="Top"
                     Width="50"/>
            <StackPanel Grid.Column="4" Grid.Row="0" Grid.RowSpan="3" Margin="10,21,0,0">
                <Button x:Name="CheckoutBtn" 
                        Content="Checkout" 
                        Click="CheckoutBtn_Click"
                        Width="91" Height="29"/>
                <Button x:Name="CheckinBtn" 
                        Content="Check-in" 
                        Click="CheckinBtn_Click" 
                        Width="91" Height="29" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel x:Name="ControllerReadout" Grid.ColumnSpan="2" Grid.Row="2" Grid.Column="0" Margin="20">
                <TextBlock x:Name="TestName"/>
                <TextBlock x:Name="TestType"/>
                <TextBlock x:Name="TestCheckedout"/>
                <TextBlock x:Name="TestStatus"/>
                <TextBlock x:Name="TestOwner"/>
            </StackPanel>

            <Button x:Name="AddItemBtn" Content="Add Item" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="3" Grid.Column="4" Height="26" Width="116" Click="AddItemBtn_Click" Grid.ColumnSpan="2"/>
            <Button x:Name="RemoveItemBtn" Content="Remove Item" HorizontalAlignment="Center" Margin="0,31,0,0" VerticalAlignment="Top" Grid.Row="3" Grid.Column="4" Height="26" Width="116" Click="RemoveItemBtn_Click" Grid.ColumnSpan="2"/>
            <Button Style="{DynamicResource MaterialDesignFloatingActionButton}"
                    Content="{materialDesign:PackIcon Gear}"
                    Grid.Column="6" 
                    HorizontalAlignment="Left" VerticalAlignment="Top" Height="27" Width="27" Margin="29,12,0,0"/>



        </Grid>
    </Border>
</Window>
